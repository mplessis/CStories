kotlin.daemon.jvmargs=-Xmx4g
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
# Single source of truth for the published io.cstories:* coordinates.
# cstories-gradle-plugin is a separate, included Gradle build (see the root
# settings.gradle.kts `pluginManagement { includeBuild(...) }`) and can't
# read Gradle properties from this file automatically, so its
# build.gradle.kts parses this same file directly instead of duplicating
# these values.
cstoriesGroup=io.cstories
cstoriesVersion=0.7.0-SNAPSHOT
